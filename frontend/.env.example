# 开发：浏览器只访问 Vite；/api 代理到 FastAPI（默认 8001）。
# ./scripts/dev.sh 会按 BACKEND_PORT / FRONTEND_PORT 覆盖下面两项，一般不用手改。
VITE_DEV_BACKEND_URL=http://127.0.0.1:8001
# VITE_DEV_PORT=5175

# 生产/预览：静态页与 API 不同源时，设后端根地址（程序会自动补全 /api）。
# VITE_API_BASE_URL=http://127.0.0.1:8001
